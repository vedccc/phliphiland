# Phillip Island Host — Guest Agent

AI guest-messaging agent for Phillip Island Host (Property Management). Receives guest
messages from booking platforms via **Guesty**, answers from a knowledge base, raises
maintenance tickets, processes extra requests, and escalates to a human when needed.
Backend: Trigger.dev + Claude + Supabase. Dashboard: Vite/React.
